spring:
  application:
    name: server
  profiles:
    active: dev # 环境
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        group: DEFAULT_GROUP
        file-extension: yml